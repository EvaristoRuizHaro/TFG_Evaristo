<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F4F7F9">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="👥 Mis clientes"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#00695C"
        android:layout_gravity="center"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/tvVacioClientes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Aún no tienes clientes vinculados. Pulsa \"Añadir cliente\" para comenzar."
        android:gravity="center"
        android:padding="24dp"
        android:textColor="#9E9E9E"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMisClientes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <Button
        android:id="@+id/btnAnadirCliente"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:text="+ Añadir cliente"
        android:backgroundTint="#00695C"
        android:textColor="#FFFFFF"
        app:cornerRadius="8dp"
        android:layout_marginBottom="8dp" />

    <Button
        android:id="@+id/btnVolverClientes"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="Volver"
        android:backgroundTint="#757575"
        android:textColor="#FFFFFF"
        app:cornerRadius="8dp" />
</LinearLayout>
